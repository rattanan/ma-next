"use client";

import Link from "next/link";
import { useCallback, useEffect, useState, type FormEvent, type ReactNode } from "react";
import { ArrowLeft, Camera, Download, Loader2, PauseCircle, Play, Printer, RefreshCw, WifiOff } from "lucide-react";
import { Alert } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { cn } from "@/lib/utils";

type Overview = { users: Array<{ id: string; fullName: string }>; departments: Array<{ id: string; name: string }>; spareParts: Array<{ id: string; code: string; name: string; unit: string | null }> };
type Detail = { order: Record<string, unknown> & { id: string; code: string; title: string; description: string; workType: string; status: string; priority: string; severity: string; equipmentOperatingStatus: string; assetCode: string; assetName: string; assignedTo: string | null; departmentId: string | null; crewName: string | null; dueAt: string | null; plannedStartAt: string | null; plannedFinishAt: string | null; estimatedMinutes: number | null; backlogReason: string | null; notes: string | null }; tasks: Array<Record<string, unknown> & { id: string; sequence: number; kind: string; title: string; description: string | null; required: boolean; status: string; responseType: string | null; responseValue: string | null; result: string | null }>; execution: Array<{ id: string; description: string; departmentId: string | null; employeeId: string | null; positionName: string | null; workType: string | null; minutesSpent: number; overtimeMinutes: number; overtimeMultiplier: string; actionAt: string }>; usedSpareParts: Array<{ id: string; code: string; name: string; quantity: string; unit: string | null; transactionType: string; warehouse: string | null; storageLocation: string | null; referenceDocument: string | null; note: string | null; usedAt: string }>; toolLoans: Array<{ id: string; toolCode: string; toolName: string; quantity: string; status: string; usageCondition: string | null; issuedAt: string | null; returnedAt: string | null; notes: string | null }>; acceptances: Array<Record<string, unknown> & { id: string; acceptedAt: string; details: string; lotoReference: string | null; permitNumber: string | null; testResult: string | null; handoverDetails: string | null }>; assignments: Array<{ id: string; userId: string | null; teamName: string | null; assignmentType: string; assignedAt: string; endedAt: string | null; note: string | null }>; backlogEvents: Array<{ id: string; reason: string; enteredAt: string; resumedAt: string | null; resolution: string | null }>; completions: Array<{ id: string; result: string; solution: string; durationMinutes: number; completedAt: string }>; verifications: Array<{ id: string; decision: string; note: string; verifiedAt: string }>; events: Array<{ id: string; eventType: string; fromStatus: string | null; toStatus: string | null; note: string | null; createdAt: string }> };
const tabNames = ["overview", "planning", "job-steps", "checklist", "labor", "materials", "tools", "safety-loto", "documents", "acceptance", "completion", "history"] as const;
const label = (value: string) => value.toLowerCase().replaceAll("_", " ").replace(/(^|\s)\S/g, (letter) => letter.toUpperCase());
const format = (value?: string | null) => value ? new Date(value).toLocaleString([], { dateStyle: "medium", timeStyle: "short" }) : "Not set";
const can = (permissions: string[], permission: string) => permissions.includes(permission);

export default function WorkOrderDetail({ id, permissions }: { id: string; permissions: string[] }) {
  const [detail, setDetail] = useState<Detail | null>(null); const [overview, setOverview] = useState<Overview>({ users: [], departments: [], spareParts: [] }); const [documents, setDocuments] = useState<Array<{ id: string; originalName: string; contentType: string; createdAt: string }>>([]);
  const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(""); const [message, setMessage] = useState(""); const [error, setError] = useState("");
  const load = useCallback(async () => { setLoading(true); setError(""); try { const [record, refs, docs] = await Promise.all([request(`/api/work-orders/${id}`), request("/api/maintenance/overview"), request(`/api/attachments?entityType=WORK_ORDER_DOCUMENT&entityId=${id}`).catch(() => ({ attachments: [] }))]); setDetail(record); setOverview(refs); setDocuments(docs.attachments ?? []); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to load work order"); } finally { setLoading(false); } }, [id]);
  useEffect(() => {
    let active = true;
    Promise.all([request(`/api/work-orders/${id}`), request("/api/maintenance/overview"), request(`/api/attachments?entityType=WORK_ORDER_DOCUMENT&entityId=${id}`).catch(() => ({ attachments: [] }))])
      .then(([record, refs, docs]) => { if (active) { setDetail(record); setOverview(refs); setDocuments(docs.attachments ?? []); } })
      .catch((cause) => { if (active) setError(cause instanceof Error ? cause.message : "Unable to load work order"); })
      .finally(() => { if (active) setLoading(false); });
    return () => { active = false; };
  }, [id]);
  async function command(name: string, payload: unknown, success: string) { setBusy(name); setError(""); setMessage(""); try { await request(`/api/work-orders/${id}/commands/${name}`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }); setMessage(success); await load(); return true; } catch (cause) { setError(cause instanceof Error ? cause.message : "Command failed"); return false; } finally { setBusy(""); } }
  if (loading && !detail) return <main className="space-y-4 p-4 md:p-6"><Skeleton className="h-32 rounded-2xl" /><Skeleton className="h-14 rounded-xl" /><Skeleton className="h-96 rounded-2xl" /></main>;
  if (!detail) return <main className="grid min-h-[60vh] place-items-center p-6"><Alert variant="destructive"><span>{error || "Work order not found"}</span><Button variant="outline" onClick={load}><RefreshCw className="size-4" />Retry</Button></Alert></main>;
  const { order } = detail; const executable = can(permissions, "EXECUTE_WORK_ORDERS"); const manager = can(permissions, "MANAGE_WORK_ORDERS"); const verifier = can(permissions, "VERIFY_WORK_ORDERS");
  return <main className="mx-auto max-w-[96rem] space-y-4 p-3 md:p-6">
    <Button asChild variant="ghost" className="-ml-2"><Link href="/work-orders"><ArrowLeft className="size-4" />Back to Work Orders</Link></Button>
    <Card className="overflow-hidden"><div className="h-1.5 bg-gradient-to-r from-blue-700 via-sky-500 to-emerald-500" /><CardContent className="p-5"><div className="flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><span className="text-sm font-bold text-blue-700">{order.code}</span><Status value={order.status} /><Status value={order.priority} /></div><h1 className="mt-2 text-2xl font-bold tracking-tight md:text-3xl">{order.title}</h1><p className="mt-2 text-sm text-slate-600">{order.assetCode} · {order.assetName} · {label(order.workType)}</p></div><div className="flex flex-wrap gap-2 print:hidden"><Button asChild variant="outline"><a href={`/api/work-orders/${id}/print`} target="_blank" rel="noreferrer"><Printer className="size-4" />Print</a></Button>{(order.status === "OPEN" || order.status === "BACKLOG") && executable && <Button onClick={() => command("start", {}, "Work started")} disabled={busy === "start"}><Play className="size-4" />Start</Button>}{["OPEN", "IN_PROGRESS"].includes(order.status) && executable && <QuickDialog label="Backlog" icon={<PauseCircle className="size-4" />} fields={<><FormField label="Reason"><textarea name="reason" required minLength={3} className="wo-input" /></FormField><FormField label="Expected resume"><input name="expectedResumeAt" type="datetime-local" className="wo-input" /></FormField></>} submit={(data) => command("backlog", { reason: data.get("reason"), expectedResumeAt: data.get("expectedResumeAt") ? new Date(String(data.get("expectedResumeAt"))).toISOString() : null }, "Moved to backlog")} />}{order.status === "BACKLOG" && executable && <QuickDialog label="Resume" fields={<FormField label="Resolution"><textarea name="resolution" required minLength={3} className="wo-input" /></FormField>} submit={(data) => command("resume", { resolution: data.get("resolution") }, "Work resumed")} />}</div></div>
      <div className="mt-5 grid grid-cols-2 gap-3 border-t pt-4 text-sm md:grid-cols-4"><Fact label="Department" value={departmentName(overview, order.departmentId)} /><Fact label="Crew" value={order.crewName || "Unassigned"} /><Fact label="Assignee" value={userName(overview, order.assignedTo)} /><Fact label="Due" value={format(order.dueAt)} /></div>
      <div className="mt-5 grid grid-cols-5 gap-1" aria-label="Work order status timeline">{["OPEN", "IN_PROGRESS", "COMPLETION_PENDING", "VERIFIED", "CLOSED"].map((state, index) => <div key={state} className={cn("relative text-center text-[10px] font-semibold text-slate-400 sm:text-xs", state === order.status && "text-blue-700", order.status === "BACKLOG" && index === 0 && "text-red-700")}><span className={cn("mx-auto mb-1 grid size-7 place-items-center rounded-full bg-slate-200", state === order.status && "bg-blue-700 text-white", order.status === "BACKLOG" && index === 0 && "bg-red-600 text-white")}>{index + 1}</span>{label(state)}</div>)}</div>
    </CardContent></Card>
    {message && <Alert aria-live="polite" className="border-emerald-200 bg-emerald-50 text-emerald-900">{message}</Alert>}{error && <Alert variant="destructive" aria-live="assertive">{error}</Alert>}
    <Alert className="md:hidden"><WifiOff className="size-4" /><span>Mobile execution requires a connection. Keep this page open and submit each record before leaving the work area.</span></Alert>
    <Tabs defaultValue="overview"><TabsList className="h-auto w-full justify-start gap-1 overflow-x-auto rounded-xl border bg-white p-2 print:hidden">{tabNames.map((tab) => <TabsTrigger key={tab} value={tab} className="min-h-10 whitespace-nowrap">{label(tab)}</TabsTrigger>)}</TabsList>
      <TabsContent value="overview"><Panel title="Overview"><dl className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Fact label="Description" value={order.description} wide /><Fact label="Priority" value={label(order.priority)} /><Fact label="Severity" value={label(order.severity)} /><Fact label="Equipment status" value={label(order.equipmentOperatingStatus)} /><Fact label="Planned start" value={format(order.plannedStartAt)} /><Fact label="Planned finish" value={format(order.plannedFinishAt)} /><Fact label="Estimate" value={order.estimatedMinutes ? `${order.estimatedMinutes} minutes` : "Not set"} /><Fact label="Notes" value={order.notes || "None"} wide /></dl></Panel></TabsContent>
      <TabsContent value="planning"><Panel title="Planning & assignment" subtitle="Assignment changes are retained in history.">{manager && <QuickForm submitLabel="Assign technician" submit={(data) => command("assign", { assignedTo: data.get("assignedTo"), departmentId: data.get("departmentId") || null, teamName: data.get("teamName") || null, positionName: data.get("positionName") || null, assignmentType: "TECHNICIAN", note: data.get("note") }, "Assignment updated")}><FormField label="Department"><select name="departmentId" className="wo-input"><option value="">Keep current</option>{overview.departments.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></FormField><FormField label="Technician"><select name="assignedTo" required className="wo-input"><option value="">Select technician</option>{overview.users.map((item) => <option key={item.id} value={item.id}>{item.fullName}</option>)}</select></FormField><FormField label="Team"><input name="teamName" className="wo-input" /></FormField><FormField label="Position / skill"><input name="positionName" className="wo-input" /></FormField><FormField label="Assignment note"><input name="note" required minLength={3} className="wo-input" /></FormField></QuickForm>}<Timeline items={detail.assignments.map((item) => ({ id: item.id, title: `${label(item.assignmentType)} · ${userName(overview, item.userId)}`, detail: item.note || item.teamName || "Assignment", at: item.assignedAt, end: item.endedAt }))} empty="No assignment history." /></Panel></TabsContent>
      <TabsContent value="job-steps"><TaskPanel kind="JOB_STEP" detail={detail} manager={manager} executable={executable} command={command} /></TabsContent>
      <TabsContent value="checklist"><TaskPanel kind="CHECKLIST" detail={detail} manager={manager} executable={executable} command={command} /></TabsContent>
      <TabsContent value="labor"><Panel title="Labor & overtime">{order.status === "IN_PROGRESS" && executable && <QuickForm submitLabel="Record time" submit={(data) => command("time-entry", { description: data.get("description"), departmentId: data.get("departmentId") || null, employeeId: data.get("employeeId") || null, positionName: data.get("positionName") || null, workType: data.get("workType") || null, minutesSpent: Number(data.get("minutesSpent")), overtimeMinutes: Number(data.get("overtimeMinutes") || 0), overtimeMultiplier: Number(data.get("overtimeMultiplier") || 1), actionAt: new Date(String(data.get("actionAt"))).toISOString() }, "Time entry recorded")}><FormField label="Work note"><input name="description" required minLength={3} className="wo-input" /></FormField><FormField label="Department"><select name="departmentId" className="wo-input"><option value="">Current department</option>{overview.departments.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></FormField><FormField label="Employee"><select name="employeeId" className="wo-input"><option value="">Current user</option>{overview.users.map((item) => <option key={item.id} value={item.id}>{item.fullName}</option>)}</select></FormField><FormField label="Position / skill"><input name="positionName" className="wo-input" /></FormField><FormField label="Work type"><input name="workType" placeholder="Repair, inspection…" className="wo-input" /></FormField><FormField label="Normal minutes"><input name="minutesSpent" required type="number" min={1} max={1440} className="wo-input" /></FormField><FormField label="OT minutes"><input name="overtimeMinutes" type="number" min={0} max={1440} defaultValue={0} className="wo-input" /></FormField><FormField label="OT type"><select name="overtimeMultiplier" className="wo-input"><option value="1">Normal</option><option value="1.5">1.5×</option><option value="2">2×</option><option value="3">3×</option></select></FormField><FormField label="Work date"><input name="actionAt" required type="datetime-local" defaultValue={new Date().toISOString().slice(0, 16)} className="wo-input" /></FormField></QuickForm>}<DataRows rows={detail.execution.map((item) => ({ key: item.id, main: `${item.minutesSpent} min · ${item.description}`, meta: `${item.positionName || item.workType || "Normal time"}${item.overtimeMinutes ? ` · OT ${item.overtimeMinutes} min × ${item.overtimeMultiplier}` : ""}`, at: item.actionAt }))} empty="No time entries." /></Panel></TabsContent>
      <TabsContent value="materials"><Panel title="Spare parts & material usage" subtitle="The inventory adapter records immutable usage; stock consistency remains outside the UI.">{order.status === "IN_PROGRESS" && executable && <QuickForm submitLabel="Record material" submit={(data) => command("material", { sparePartId: data.get("sparePartId"), quantity: Number(data.get("quantity")), transactionType: data.get("transactionType"), warehouse: data.get("warehouse") || null, storageLocation: data.get("storageLocation") || null, unit: data.get("unit") || null, referenceDocument: data.get("referenceDocument") || null, note: data.get("note") }, "Material transaction recorded")}><FormField label="Spare part"><select name="sparePartId" required className="wo-input"><option value="">Select part</option>{overview.spareParts.map((part) => <option key={part.id} value={part.id}>{part.code} — {part.name}</option>)}</select></FormField><FormField label="Transaction"><select name="transactionType" className="wo-input"><option value="CONSUMED">Consumed</option><option value="REQUESTED">Requested</option><option value="RESERVED">Reserved</option><option value="ISSUED">Issued</option><option value="RETURNED">Returned</option></select></FormField><FormField label="Quantity"><input name="quantity" type="number" min="0.0001" step="0.0001" required className="wo-input" /></FormField><FormField label="Unit"><input name="unit" className="wo-input" /></FormField><FormField label="Warehouse"><input name="warehouse" className="wo-input" /></FormField><FormField label="Storage location"><input name="storageLocation" className="wo-input" /></FormField><FormField label="Reference document"><input name="referenceDocument" className="wo-input" /></FormField><FormField label="Note"><input name="note" className="wo-input" /></FormField></QuickForm>}<DataRows rows={detail.usedSpareParts.map((item) => ({ key: item.id, main: `${item.code} — ${item.name}`, meta: `${label(item.transactionType)} · ${item.quantity} ${item.unit || "unit"}${item.warehouse ? ` · ${item.warehouse}` : ""}${item.referenceDocument ? ` · ${item.referenceDocument}` : ""}`, at: item.usedAt }))} empty="No material transactions." /></Panel></TabsContent>
      <TabsContent value="tools"><Panel title="Equipment & tool loan">{manager && !["COMPLETION_PENDING", "VERIFIED", "CLOSED"].includes(order.status) && <QuickForm submitLabel="Add tool" submit={(data) => command("add-tool", { toolCode: data.get("toolCode"), toolName: data.get("toolName"), quantity: Number(data.get("quantity")), usageCondition: data.get("usageCondition"), notes: data.get("notes") }, "Tool planned")}><FormField label="Tool code"><input name="toolCode" required className="wo-input" /></FormField><FormField label="Tool name"><input name="toolName" required minLength={2} className="wo-input" /></FormField><FormField label="Quantity"><input name="quantity" type="number" min="0.0001" step="0.0001" required className="wo-input" /></FormField><FormField label="Usage condition"><input name="usageCondition" className="wo-input" /></FormField><FormField label="Notes"><input name="notes" className="wo-input" /></FormField></QuickForm>}<div className="space-y-3">{detail.toolLoans.map((loan) => <article key={loan.id} className="flex flex-col gap-3 rounded-xl border p-4 sm:flex-row sm:items-center sm:justify-between"><div><strong>{loan.toolCode} — {loan.toolName}</strong><p className="text-sm text-slate-600">{loan.quantity} · {loan.usageCondition || "No condition noted"}</p></div><div className="flex items-center gap-2"><Status value={loan.status} />{executable && loan.status === "PLANNED" && <Button size="sm" onClick={() => command("tool-command", { loanId: loan.id, command: "ISSUE", note: "Issued for work" }, "Tool issued")}>Issue</Button>}{executable && loan.status === "ISSUED" && <Button size="sm" onClick={() => command("tool-command", { loanId: loan.id, command: "RETURN", note: "Returned after work" }, "Tool returned")}>Return</Button>}</div></article>)}{detail.toolLoans.length === 0 && <Empty text="No tools planned." />}</div></Panel></TabsContent>
      <TabsContent value="safety-loto"><AcceptancePanel order={order} acceptances={detail.acceptances} executable={executable} command={command} safetyOnly /></TabsContent>
      <TabsContent value="documents"><DocumentPanel id={id} documents={documents} permitted={can(permissions, "MANAGE_ATTACHMENTS")} reload={load} setError={setError} /></TabsContent>
      <TabsContent value="acceptance"><AcceptancePanel order={order} acceptances={detail.acceptances} executable={executable} command={command} /></TabsContent>
      <TabsContent value="completion"><CompletionPanel detail={detail} executable={executable} verifier={verifier} closer={can(permissions, "CLOSE_WORK_ORDERS")} command={command} /></TabsContent>
      <TabsContent value="history"><Panel title="Append-only Work Order history"><Timeline items={detail.events.map((item) => ({ id: item.id, title: label(item.eventType), detail: item.note || `${item.fromStatus ? label(item.fromStatus) : "Created"} → ${item.toStatus ? label(item.toStatus) : "Recorded"}`, at: item.createdAt }))} empty="No events recorded." /><div className="mt-4 flex flex-wrap gap-2"><Button variant="outline" asChild><a href={`/api/work-orders/${id}/print?format=json`} target="_blank" rel="noreferrer"><Download className="size-4" />Export JSON</a></Button><Button variant="outline" asChild><a href={`/api/work-orders/${id}/print?report=tool`} target="_blank" rel="noreferrer"><Printer className="size-4" />Tool form</a></Button><Button variant="outline" asChild><a href={`/api/work-orders/${id}/print?report=material`} target="_blank" rel="noreferrer"><Printer className="size-4" />Material form</a></Button></div></Panel></TabsContent>
    </Tabs>
  </main>;
}

async function request(url: string, options?: RequestInit) { const response = await fetch(url, options); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || "Request failed"); return body; }
async function uploadEvidence(files: File[], entityType: string, entityId: string) { return Promise.all(files.map(async (file) => { const form = new FormData(); form.set("file", file); form.set("entityType", entityType); form.set("entityId", entityId); const body = await request("/api/attachments/upload", { method: "POST", body: form }); return body.attachment.id as string; })); }
function Status({ value }: { value: string }) { return <Badge className={cn("border", value === "BACKLOG" || value === "CRITICAL" ? "border-red-200 bg-red-50 text-red-800" : value === "CLOSED" || value === "VERIFIED" || value === "COMPLETED" ? "border-emerald-200 bg-emerald-50 text-emerald-800" : "border-blue-200 bg-blue-50 text-blue-800")}>{label(value)}</Badge>; }
function Panel({ title, subtitle, children }: { title: string; subtitle?: string; children: ReactNode }) { return <Card className="mt-4"><CardContent className="p-4 md:p-6"><h2 className="text-lg font-bold md:text-xl">{title}</h2>{subtitle && <p className="mt-1 text-sm text-slate-600">{subtitle}</p>}<div className="mt-5">{children}</div></CardContent></Card>; }
function Fact({ label: name, value, wide }: { label: string; value: ReactNode; wide?: boolean }) { return <div className={wide ? "sm:col-span-2 lg:col-span-4" : ""}><dt className="text-xs font-bold uppercase tracking-wide text-slate-500">{name}</dt><dd className="mt-1 whitespace-pre-wrap font-medium">{value}</dd></div>; }
function FormField({ label: name, children }: { label: string; children: ReactNode }) { return <label className="grid gap-1.5 text-sm font-semibold"><span>{name}</span>{children}</label>; }
function QuickForm({ children, submit, submitLabel }: { children: ReactNode; submit: (data: FormData) => Promise<unknown>; submitLabel: string }) { const [submitting, setSubmitting] = useState(false); async function onSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSubmitting(true); const ok = await submit(new FormData(event.currentTarget)); if (ok) event.currentTarget.reset(); setSubmitting(false); } return <form onSubmit={onSubmit} className="mb-5 grid gap-3 rounded-xl border bg-slate-50 p-4 sm:grid-cols-2 lg:grid-cols-3">{children}<div className="flex items-end"><Button className="min-h-11 w-full" disabled={submitting}>{submitting && <Loader2 className="size-4 animate-spin" />}{submitLabel}</Button></div></form>; }
function QuickDialog({ label: name, icon, fields, submit }: { label: string; icon?: ReactNode; fields: ReactNode; submit: (data: FormData) => Promise<unknown> }) { const [open, setOpen] = useState(false); return <div className="relative"><Button variant="outline" onClick={() => setOpen((v) => !v)}>{icon}{name}</Button>{open && <div className="absolute right-0 top-12 z-20 w-80 rounded-xl border bg-white p-4 shadow-xl"><QuickForm submitLabel={name} submit={async (data) => { const result = await submit(data); if (result) setOpen(false); return result; }}>{fields}</QuickForm></div>}</div>; }
function DataRows({ rows, empty }: { rows: Array<{ key: string; main: string; meta: string; at: string }>; empty: string }) { return <div className="divide-y rounded-xl border">{rows.map((item) => <article key={item.key} className="grid gap-1 p-4 sm:grid-cols-[1fr_auto]"><strong>{item.main}</strong><time className="text-xs text-slate-500">{format(item.at)}</time><p className="text-sm text-slate-600 sm:col-span-2">{item.meta}</p></article>)}{rows.length === 0 && <Empty text={empty} />}</div>; }
function Empty({ text }: { text: string }) { return <p className="rounded-xl border border-dashed p-6 text-center text-sm text-slate-500">{text}</p>; }
function Timeline({ items, empty }: { items: Array<{ id: string; title: string; detail: string; at: string; end?: string | null }>; empty: string }) { if (!items.length) return <Empty text={empty} />; return <ol className="relative ml-2 border-l border-slate-200">{items.map((item) => <li key={item.id} className="relative ml-5 pb-5"><span className="absolute -left-[1.55rem] top-1 size-3 rounded-full border-2 border-white bg-blue-600" /><strong>{item.title}</strong><p className="mt-1 text-sm text-slate-600">{item.detail}</p><time className="text-xs text-slate-500">{format(item.at)}{item.end ? ` — ${format(item.end)}` : ""}</time></li>)}</ol>; }
function userName(overview: Overview, id: string | null) { return overview.users.find((user) => user.id === id)?.fullName || "Unassigned"; }
function departmentName(overview: Overview, id: string | null) { return overview.departments.find((item) => item.id === id)?.name || "Unassigned"; }

function TaskPanel({ kind, detail, manager, executable, command }: { kind: string; detail: Detail; manager: boolean; executable: boolean; command: (name: string, payload: unknown, success: string) => Promise<boolean> }) { const rows = detail.tasks.filter((task) => task.kind === kind); return <Panel title={kind === "CHECKLIST" ? "Checklist" : "Job Steps"} subtitle="Required items block completion until recorded as complete.">{manager && !["COMPLETION_PENDING", "VERIFIED", "CLOSED"].includes(detail.order.status) && <QuickForm submitLabel={`Add ${kind === "CHECKLIST" ? "item" : "step"}`} submit={(data) => command("add-task", { kind, title: data.get("title"), description: data.get("description"), required: data.get("required") === "on", responseType: kind === "CHECKLIST" ? data.get("responseType") || "TEXT" : null, estimatedMinutes: data.get("estimatedMinutes") ? Number(data.get("estimatedMinutes")) : null }, "Item added")}><FormField label="Title"><input name="title" required minLength={2} className="wo-input" /></FormField><FormField label="Instruction"><input name="description" className="wo-input" /></FormField>{kind === "CHECKLIST" ? <FormField label="Response type"><select name="responseType" className="wo-input"><option>TEXT</option><option>NUMBER</option><option>PASS_FAIL</option><option>YES_NO</option><option>OPTION</option></select></FormField> : <FormField label="Estimated minutes"><input name="estimatedMinutes" type="number" min={0} className="wo-input" /></FormField>}<label className="flex min-h-11 items-center gap-2 text-sm font-semibold"><input name="required" type="checkbox" defaultChecked />Required</label></QuickForm>}<div className="space-y-3">{rows.map((task) => <article key={task.id} className="rounded-xl border p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><span className="text-xs font-bold text-blue-700">#{task.sequence}</span><h3 className="font-bold">{task.title}</h3><p className="text-sm text-slate-600">{task.description || "No instruction"}</p></div><div className="flex flex-wrap items-center gap-2"><Status value={task.status} />{executable && task.status !== "COMPLETED" && task.status !== "BACKLOG" && <QuickDialog label="Backlog" fields={<FormField label="Reason"><input name="reason" required minLength={3} className="wo-input" /></FormField>} submit={(data) => command("task-backlog", { taskId: task.id, reason: data.get("reason") }, "Task moved to backlog")} />}{executable && task.status === "BACKLOG" && <QuickDialog label="Resume" fields={<FormField label="Resolution"><input name="resolution" required minLength={3} className="wo-input" /></FormField>} submit={(data) => command("task-resume", { taskId: task.id, resolution: data.get("resolution") }, "Task resumed")} />}</div></div>{task.responseValue && <p className="mt-3 rounded-lg bg-slate-50 p-3 text-sm">Response: {task.responseValue}</p>}{executable && ["OPEN", "BACKLOG", "IN_PROGRESS"].includes(detail.order.status) && task.status !== "COMPLETED" && task.status !== "BACKLOG" && <QuickForm submitLabel="Complete" submit={(data) => command("task-status", { taskId: task.id, status: "COMPLETED", result: data.get("result"), responseValue: data.get("responseValue"), remarks: data.get("remarks"), actualMinutes: data.get("actualMinutes") ? Number(data.get("actualMinutes")) : null }, "Item completed")}><FormField label={kind === "CHECKLIST" ? "Response" : "Result"}><input name={kind === "CHECKLIST" ? "responseValue" : "result"} required className="wo-input" /></FormField><FormField label="Remarks"><input name="remarks" className="wo-input" /></FormField><FormField label="Actual minutes"><input name="actualMinutes" type="number" min={0} className="wo-input" /></FormField></QuickForm>}</article>)}{!rows.length && <Empty text={`No ${kind === "CHECKLIST" ? "checklist items" : "job steps"}.`} />}</div></Panel>; }

function AcceptancePanel({ order, acceptances, executable, command, safetyOnly = false }: { order: Detail["order"]; acceptances: Detail["acceptances"]; executable: boolean; command: (name: string, payload: unknown, success: string) => Promise<boolean>; safetyOnly?: boolean }) { return <Panel title={safetyOnly ? "Safety & LOTO" : "Accept Execution"} subtitle="Safety information is preserved as submitted evidence.">{order.status === "IN_PROGRESS" && executable && <QuickForm submitLabel="Record acceptance" submit={(data) => command("acceptance", { acceptedAt: new Date(String(data.get("acceptedAt"))).toISOString(), details: data.get("details"), notes: data.get("notes"), lotoReference: data.get("lotoReference"), isolationPoints: data.get("isolationPoints"), permitNumber: data.get("permitNumber"), safetyInstructions: data.get("safetyInstructions"), hazards: data.get("hazards"), operatingConditions: data.get("operatingConditions"), logSheetReference: data.get("logSheetReference"), testResult: data.get("testResult"), handoverDetails: data.get("handoverDetails"), attachmentIds: [] }, "Execution acceptance recorded")}><FormField label="Accepted date"><input name="acceptedAt" type="datetime-local" required defaultValue={new Date().toISOString().slice(0, 16)} className="wo-input" /></FormField><FormField label="Details"><input name="details" required minLength={3} className="wo-input" /></FormField><FormField label="LOTO reference"><input name="lotoReference" className="wo-input" /></FormField><FormField label="Isolation points"><input name="isolationPoints" className="wo-input" /></FormField><FormField label="Permit number"><input name="permitNumber" className="wo-input" /></FormField><FormField label="Hazards"><input name="hazards" className="wo-input" /></FormField><FormField label="Safety instruction"><input name="safetyInstructions" className="wo-input" /></FormField>{!safetyOnly && <><FormField label="Operating conditions"><input name="operatingConditions" className="wo-input" /></FormField><FormField label="Log sheet"><input name="logSheetReference" className="wo-input" /></FormField><FormField label="Test result"><input name="testResult" className="wo-input" /></FormField><FormField label="Handover"><input name="handoverDetails" className="wo-input" /></FormField></>}<FormField label="Notes"><input name="notes" className="wo-input" /></FormField></QuickForm>}<div className="space-y-3">{acceptances.map((item) => <article key={item.id} className="rounded-xl border p-4"><div className="flex justify-between gap-3"><strong>{item.details}</strong><time className="text-xs text-slate-500">{format(item.acceptedAt)}</time></div><dl className="mt-3 grid gap-3 text-sm sm:grid-cols-2"><Fact label="LOTO" value={item.lotoReference || "None"} /><Fact label="Permit" value={item.permitNumber || "None"} /><Fact label="Test" value={item.testResult || "Not recorded"} /><Fact label="Handover" value={item.handoverDetails || "Not recorded"} /></dl></article>)}{!acceptances.length && <Empty text="No acceptance or safety record." />}</div></Panel>; }

function DocumentPanel({ id, documents, permitted, reload, setError }: { id: string; documents: Array<{ id: string; originalName: string; contentType: string; createdAt: string }>; permitted: boolean; reload: () => Promise<void>; setError: (value: string) => void }) { async function upload(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const file = new FormData(event.currentTarget).get("file"); if (!(file instanceof File) || !file.size) return; const form = new FormData(); form.set("file", file); form.set("entityType", "WORK_ORDER_DOCUMENT"); form.set("entityId", id); try { await request("/api/attachments/upload", { method: "POST", body: form }); await reload(); event.currentTarget.reset(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Upload failed"); } } return <Panel title="Documents" subtitle="Images are stored through the shared attachment service; no file binary is stored on the Work Order.">{permitted && <form onSubmit={upload} className="mb-5 flex flex-col gap-3 rounded-xl border bg-slate-50 p-4 sm:flex-row sm:items-end"><FormField label="Image document (JPEG, PNG or WebP; max 5 MB)"><input name="file" type="file" accept="image/jpeg,image/png,image/webp" required className="wo-input" /></FormField><Button><Camera className="size-4" />Upload</Button></form>}<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{documents.map((document) => <a key={document.id} href={`/api/attachments/${document.id}/content`} target="_blank" rel="noreferrer" className="rounded-xl border p-4 hover:border-blue-300"><strong>{document.originalName}</strong><p className="text-xs text-slate-500">{document.contentType} · {format(document.createdAt)}</p></a>)}{!documents.length && <Empty text="No documents attached." />}</div></Panel>; }

function CompletionPanel({ detail, executable, verifier, closer, command }: { detail: Detail; executable: boolean; verifier: boolean; closer: boolean; command: (name: string, payload: unknown, success: string) => Promise<boolean> }) { const completion = detail.completions[0]; async function submitCompletion(data: FormData) { const before = data.getAll("beforePhotos").filter((item): item is File => item instanceof File && item.size > 0); const after = data.getAll("afterPhotos").filter((item): item is File => item instanceof File && item.size > 0); const [beforePhotoAttachmentIds, afterPhotoAttachmentIds] = await Promise.all([uploadEvidence(before, "WORK_ORDER_BEFORE", detail.order.id), uploadEvidence(after, "WORK_ORDER_AFTER", detail.order.id)]); return command("completion", { result: data.get("result"), problem: data.get("problem"), cause: data.get("cause"), solution: data.get("solution"), escalation: data.get("escalation"), notes: data.get("notes"), durationMinutes: Number(data.get("durationMinutes")), beforePhotoAttachmentIds, afterPhotoAttachmentIds }, "Completion submitted"); } return <Panel title="Completion & supervisor verification">{detail.order.status === "IN_PROGRESS" && executable && <QuickForm submitLabel="Submit completion" submit={submitCompletion}><FormField label="Result"><input name="result" required minLength={2} className="wo-input" /></FormField><FormField label="Problem / failure"><input name="problem" className="wo-input" /></FormField><FormField label="Cause"><input name="cause" className="wo-input" /></FormField><FormField label="Corrective action / solution"><textarea name="solution" required minLength={3} className="wo-input" /></FormField><FormField label="Follow-up / escalation"><input name="escalation" className="wo-input" /></FormField><FormField label="Actual duration (minutes)"><input name="durationMinutes" type="number" min={1} required className="wo-input" /></FormField><FormField label="Before-work photos"><input name="beforePhotos" type="file" accept="image/jpeg,image/png,image/webp" multiple className="wo-input" /></FormField><FormField label="After-work photos"><input name="afterPhotos" type="file" accept="image/jpeg,image/png,image/webp" multiple className="wo-input" /></FormField><FormField label="Completion notes"><textarea name="notes" className="wo-input" /></FormField></QuickForm>}{completion && <article className="mb-5 rounded-xl border bg-slate-50 p-4"><strong>{completion.result}</strong><p className="mt-1 text-sm">{completion.solution}</p><p className="mt-2 text-xs text-slate-500">{completion.durationMinutes} minutes · {format(completion.completedAt)}</p></article>}{detail.order.status === "COMPLETION_PENDING" && verifier && completion && <QuickForm submitLabel="Record decision" submit={(data) => command("verification", { completionId: completion.id, decision: data.get("decision"), note: data.get("note") }, "Supervisor decision recorded")}><FormField label="Decision"><select name="decision" className="wo-input"><option>VERIFIED</option><option>RETURNED</option></select></FormField><FormField label="Verification / rejection reason"><input name="note" required minLength={3} className="wo-input" /></FormField></QuickForm>}{detail.order.status === "VERIFIED" && closer && <QuickForm submitLabel="Close Work Order" submit={(data) => command("close", { note: data.get("note") }, "Work order closed")}><FormField label="Closure note"><input name="note" required minLength={3} className="wo-input" /></FormField></QuickForm>}<Timeline items={detail.verifications.map((item) => ({ id: item.id, title: label(item.decision), detail: item.note, at: item.verifiedAt }))} empty="No supervisor decision." /></Panel>; }
