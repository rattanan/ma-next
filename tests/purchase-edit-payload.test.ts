import { describe, expect, it } from "vitest";
import { purchaseEditPayload, type EditablePurchase } from "@/lib/purchasing/edit-payload";
const document: EditablePurchase = { id:"doc",status:"RETURNED_FOR_REVISION",departmentId:"dept",currencyCode:"USD",exchangeRateToThb:"35.123456",remark:"original note",internalRemark:"internal",purchaseRequestId:"pr",vendorId:"vendor",purchaseMethod:"quotation",headerDiscountAmount:"12.000001",vatAmount:"7",candidateVendors:[{vendorId:"second",rank:2},{vendorId:"first",rank:1}],lines:[{id:"line",stockItemId:"stock",stockCodeSnapshot:"S1",description:"original",quantity:"2.000001",unitPrice:"100.123456",estimatedUnitPrice:"90.123456",discountAmount:"1.000001",itemDiscountAmount:"2.000001",remark:"line note"}] };
describe("purchase edits",()=>{
 it("preserves PO reference, internal notes and exact decimal strings",()=>{const payload=purchaseEditPayload("order",document);expect(payload).toMatchObject({purchaseRequestId:"pr",vendorId:"vendor",internalRemark:"internal",remark:"original note",headerDiscountAmount:"12.000001",lines:[{stockItemId:"stock",quantity:"2.000001",unitPrice:"100.123456",itemDiscountAmount:"2.000001",remark:"line note"}]});expect(payload).not.toHaveProperty("status");});
 it("preserves ranked vendors and PR line notes without mutating source order",()=>{expect(purchaseEditPayload("request",document)).toMatchObject({candidateVendorIds:["first","second"],lines:[{estimatedUnitPrice:"90.123456",discountAmount:"1.000001",remark:"line note"}]});expect(document.candidateVendors?.[0].vendorId).toBe("second");});
 it("sends edited quantities while retaining unrelated fields",()=>{const edited={...document,lines:document.lines.map(line=>({...line,quantity:"5"}))};expect(purchaseEditPayload("order",edited)).toMatchObject({departmentId:"dept",currencyCode:"USD",exchangeRateToThb:"35.123456",lines:[{quantity:"5",unitPrice:"100.123456"}]});});
});
